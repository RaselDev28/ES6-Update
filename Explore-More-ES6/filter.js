const numbers = [12, 12, 11, 10, 4, 2, 43];

const selected = numbers.filter(p => p % 2 === 1);

console.log(selected);

const names = ['Rasel', 'Rahim', 'karim', 'sujon'];

const evenName = names.filter(name => name.length % 2 === 0);

console.log(evenName);